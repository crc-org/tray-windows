# Appveyor config for CodeReady Containers windows tray
---
image: Visual Studio 2019

configuration: Release

before_build:
  ps: nuget restore CRCTray.sln

build:
  project: CRCTray.sln

after_build:
  ps: ls bin\

artifacts:
  - path: bin\Release
    name: crc-tray
